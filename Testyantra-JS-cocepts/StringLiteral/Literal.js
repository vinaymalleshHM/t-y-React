const person = {
    name : 'Prabhas',
    age : 40,
    color : 'white'

}
// Using temple literal (String interpolation)

console.log(`Name is ${person.name}
Age is ${person.age}
color is ${person.color}
Income is ${250000000 * 12 }
        `)

let name = 'Dimple';

for (let i =name.length ; i > 0 ; i--) {

    console.log(name[i])
}